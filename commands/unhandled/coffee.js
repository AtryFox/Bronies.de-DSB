const roles = require('../../config/roles');

exports.config = {
    handled: false,
};

exports.help = {
    name: 'coffee',
    description: 'Eine Kanne Kaffe in den Chat stellen.',
    usage: ['!coffee']
};