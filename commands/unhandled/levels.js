const roles = require('../../config/roles');

exports.config = {
    handled: false,
};

exports.help = {
    name: 'levels',
    description: 'Zeigt das Leaderboard des Servers an.',
    usage: ['!levels']
};